# Greenfield block (cells A, C)
- Start with `gsd-new-project` → `gsd-new-milestone` before any phase work.
- No existing `.planning/` expected. Do NOT run `gsd-map-codebase` or `gsd-ingest-docs` — those are brownfield.
- Declare the canonical pipeline tail explicitly in this CLAUDE.md (it does not exist yet).
